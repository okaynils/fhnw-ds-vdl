# @package _global_

_base_: ../config.yaml

experiment_name: "overfit_unet_baseline"

model:
  _target_: core.UNet_Baseline
  num_classes: 18

trainer:
  run_name: "overfit_unet_baseline"
  epochs: 5000

dataset:
  overfit_test: true